package arrays;

public class Find_three_largest_element {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		//find three largest elemet from an array time:-O(n) and space:-O(1);
		//but it doesn't print the duplicate value;
		int arr[]= {12,34,98,23,11,83,24,98};
		int n=arr.length;
		int k=3;
		
		
		
		
		
		
		
		
		/*
		//its best when u know the how many elements u have to printed or return;
		int first,second,third;
		first=second=third=Integer.MIN_VALUE;
		
		for(int i=0;i<n;i++) {
			if(arr[i]>=first) {
				first=arr[i];
			}
		}
		
		for(int i=0;i<n;i++) {
			if(arr[i]>=second && arr[i]<first) {
				second=arr[i];
			}
		}
		
		for(int i=0;i<n;i++) {
			if(arr[i]>=third && arr[i]<second) {
				third=arr[i];
			}
		}
		
		System.out.print(first+" "+second+" "+third);
		
		*/
	}

}
